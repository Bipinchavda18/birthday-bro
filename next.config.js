// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
    allowedDevOrigins: [
      'http://localhost:3000',
      'https://your-allowed-origin.com',
    ],
  };
  
  module.exports = nextConfig;
  